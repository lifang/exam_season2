<%= javascript_include_tag "similarities" %>
<div class="main_title">
  <div class="m_t_h">编辑真题</div>
</div>
<%= form_for :similarity ,:url=>"/similarities/#{@similarity.id}", :method => "put",
    :html => {:onsubmit => "return new_similarity_form()"} do |f|%>
<div class="text_box">
  <div class="bianji_ny">
    <input type="hidden" name="category_id" id="category_id" value="<%= params[:category] %>" />
    <div>名称 <input name="title" id="title" type="text" size="50" value="<%= @similarity.title %>" /></div>
    <input type="hidden" name="paper_id" id="paper_id" value="<%= @paper_ids.join(",") %>" />
    <div id="selected_papers">
      <%= render :partial => "/common/selected_papers" %>
    </div>
    <div><input name="is_free" id="is_free" type="checkbox" value="1" /> 免费使用</div>
    <div><button type="submit" class="t_btn">保存</button></div>
  </div>
</div>
<% end %>
<%= render :partial => "/common/add_expaper" %>